__pycache__/
*.pyc
*.egg-info/
.venv/

# Written into the Pages artifact at deploy time, never committed.
web/firmware/

# Generated by tests/make_samples.py, and by tools/devserver.py --debug on
# startup. Served under /__debug__/ by that server, never committed, and outside
# web/ so the Pages artifact could not carry them even if they were.
/samples/
web/samples/
